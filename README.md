# Jesse Frohlich's personal webpage

## Building

First, compile all helper Haskell scripts:

```
$ ghc getTeaser.hs
$ ghc sortPosts.hs
```

Then, run `./build-site`. The static site will be generated in `build/`.
